extends layout.pug

block content
  a(href="http://localhost:3000/register") Registrarse
  table
    thead
      tr
        th Name
        th Email
    tbody
      each val in users
        tr
          td= val.name
          td= val.email